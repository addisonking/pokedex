# Pokedex Tracker

Web app to track regional & national Pokédex progress for gens 1–5. Per-Pokémon
Serebii links contextual to the game being tracked.

## Commands

- `bun install` — install deps
- `bun run dev` — start dev server
- `bun run build` — typecheck + production build (outputs to `dist/`)
- `bun run typecheck` — `tsc --noEmit`
- `bun run lint` — `biome check src`
- `bun run format` — `biome format --write src`
- `bun run generate` — regenerate `src/data/*.json` from PokeAPI (649 calls + 8 regional dexes)
- `bun run sprites` — download sprites 1–649 into `public/sprites/` from PokeAPI sprites repo (idempotent)

## Stack

Vite + React 18 + TypeScript + Tailwind v4 + Biome. React Router (HashRouter, so
static hosts work without SPA fallback config).

## State

Progress stored in `localStorage` under key `pokedex`. Shape (see `src/types.ts`):

```
{
  playthroughs: Playthrough[]   // one per save file
  progress: Record<string, TrackerState>  // keyed by playthrough id
}
Playthrough = { id, gameId, version, name, mode: "caught"|"seen", createdAt, setupDone }
TrackerState = Record<number, DexRecord>  // sparse; only s=1|2 entries stored
DexRecord = { s: 0|1|2, t?: number }      // 0 unseen / 1 seen / 2 caught; t = last change ts
```

Modes: `seen` cycles 0→1→2→clear; `caught` toggles 0↔2 (seen entries render as
unseen). `setupDone` flips after the setup banner's Done button — marks made
before that carry no `t` (baseline). Export/import JSON via Settings menu on home.

## Save importing (gen 1-5)

`src/lib/save-parser/` — parses .sav files to extract Pokédex seen/owned bitfields.
`importSave(buf: ArrayBuffer)` → `{ gameId, version, name, seen[], caught[] } | null`.
Auto-detects gen via file size + checksum/signature validation:
- Gen 1 (32KB): 8-bit inverted sum checksum, no crypto
- Gen 2 (32KB): u16 checksum, two slots, no crypto
- Gen 3 (128KB): plaintext 14-section layout, signature 0x08012025, section 0 = dex
- Gen 4 (512KB): plaintext blocks, magic 0x20060623, CRC16-CCITT, Zukan4 structure
- Gen 5 (512KB): plaintext blocks, CRC16-CCITT footer validation, Zukan5 structure
Offsets from PKHeX source (kwsch/PKHeX). Gen 4-5 distinguished by footer magic vs CRC.
Dex bitfields: LSB-first, bit N = national ID N+1. Home page "Import .sav" button
calls importSave → addPlaythrough → bulkSet(seen,1) → bulkSet(caught,2) →
completeSetup (imported marks are baseline, no `t` timestamp).

## Data

`src/data/pokemon.json` — national 1–649: `{ id, name, types, gen }`.
`src/data/games.json` — 8 games, each with `serebiiSuffix` + `dex: [{ r, n }]`
(`r` = regional number, `n` = national id). Generated by `scripts/generate-data.ts`.
Serebii location URL: `serebii.net/pokedex-{suffix}/location/{NNN}.shtml#{anchor}` — deep-links
to the in-depth location page for the playthrough's version. Version→anchor map in
`src/lib/serebii.ts` (e.g. gold→g, ruby→ruby, heartgold→hg). Gen 1 suffix is empty.
`public/sprites/{id}.png` — pixel sprites for 1–649, vendored locally (no runtime dep on
GitHub). Downloaded by `scripts/download-sprites.ts`. `PokemonCard` serves them at
`/sprites/{id}.png`.

## Game → regional dex source (PokeAPI pokedex id)

- rby / frlg → kanto (2)
- gsc → original-johto (3)
- rse → hoenn (4)
- dpp → extended-sinnoh (6, Platinum's 210)
- hgss → updated-johto (7)
- bw → original-unova (8)
- b2w2 → updated-unova (9)
